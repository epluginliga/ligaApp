module.exports = {
  root: true,
  extends: '@react-native',
  rules: {
     quotes: ['error', 'single'],
     'eol-last': true,
     semi: ['error', 'always'],
     indent: ['error', 3], // Configura a indentação como 3 espaços.
  },
};
